name: Wait for Codex

# INTERIM VARIANT — pull_request, not pull_request_target, deliberately.
# This repo has no wait-for-codex.yml on main yet, and pull_request_target
# always reads its definition from the BASE branch — switching to it on a
# branch that's ADDING the file for the first time goes dark (neither
# trigger type fires: pull_request no longer matches this file's own `on:`,
# and pull_request_target has nothing on base to match either). Land THIS
# pull_request-triggered version first so main has a working baseline, then
# a fast follow-up PR switches to pull_request_target for the real security
# reason documented in that PR (a same-repo PR modifying the merged file
# could otherwise exfiltrate CODEX_REVIEW_PAT or self-resolve its own
# review threads before human review — see HagaleTechnologies/harbor#59/#60).
on:
  # `reopened` included, but routed through the explicit re-review request
  # path below (same as synchronize), not treated as an implicit-review
  # trigger: Codex's own docs say automatic review fires on opened/
  # ready_for_review only, never on reopened, so a bare `reopened` listed
  # here with no explicit request would just poll 70 minutes for a signal
  # that never comes (Codex P2, flagged reviewing an earlier version of
  # this file — the reason reopened was dropped originally). But dropping
  # it entirely left a WORSE gap: no event here re-runs this required check
  # on reopen at all, so a PR reopened with a new head (commits pushed
  # while it was closed) stays permanently stuck on a stale/absent result
  # with no path forward except another push (Codex P2, flagged reviewing
  # THIS file on cqdx#259). Explicitly requesting a review on reopen (like
  # synchronize does) fixes both gaps at once.
  pull_request:
    types: [opened, ready_for_review, synchronize, reopened]
  # merge_group: repos using a GitHub merge queue re-evaluate every required
  # check against a temporary merge-group SHA that has no associated PR
  # context — this workflow's PR-specific job never matches that event, so
  # a required "Wait for Codex Review" check would sit permanently pending
  # on the queue SHA and stall the queue (Codex P1, flagged reviewing this
  # file on skimmer, the first repo in this rollout using a merge queue).
  # Handled by an event-gated pass-through step in the job below (not a
  # separate job — an earlier version split this into two jobs sharing the
  # same check name, which Codex flagged as relying on undocumented
  # same-name multi-run matching semantics for a REQUIRED check; see the
  # job-level comment) — the merge-group SHA already represents code that
  # passed a real Codex review during its own PR phase; re-reviewing a
  # synthetic integration commit is neither meaningful nor possible (there
  # is no PR to post a comment on or resolve threads against).
  merge_group:
    types: [checks_requested]

permissions:
  contents: read
  pull-requests: write
  issues: read

# A newer push must supersede an older run outright rather than let the old
# run finish and mutate review threads based on a review of a commit that's
# no longer HEAD — cancelling is simpler and stronger than re-verifying HEAD
# right before the mutation (Codex P2, this file's own auto-merge rollout).
concurrency:
  # REVERTED from a head-SHA-keyed group (tried on banco's sync PR): that
  # design fixed a stale-manual-rerun-cancels-the-current-run P2, but
  # Codex's own review of it immediately found a worse P1 — once different
  # heads stop sharing a group, an old run for a superseded head is no
  # longer cancelled at all, so it can run to completion, find a signal
  # tied to its OWN stale HEAD_SHA, and execute the auto-resolve mutations
  # against the PR's CURRENT threads based on that obsolete state. Back to
  # PR-number keying: a stale manual rerun cancelling a live run is a rare,
  # human-triggered, fully recoverable nuisance (re-trigger the check) —
  # strictly better than a stale run silently mutating current review
  # threads, which the head-SHA design could not fully rule out without
  # re-deriving live-head correctness at every mutation point (Codex P1,
  # flagged reviewing this file on banco's sync PR, round 2 — a direct
  # regression finding on round 1's own fix).
  group: wait-for-codex-${{ github.event.pull_request.number || github.event.merge_group.head_sha }}
  # Restricted to run_attempt == 1 (a genuinely fresh trigger): a manual
  # rerun of an OLD attempt (e.g. after a transient Codex error) replays the
  # ORIGINAL event and shares this same group — without this restriction it
  # could cancel a run that's actively gating a NEWER head before that run's
  # own head-check step gets a chance to no-op the stale rerun instead,
  # leaving the current head's required check cancelled until another push
  # or manual rerun (Codex P2, flagged reviewing this file on
  # widdershins#114). A fresh run (attempt 1) still cancels a stale
  # in-progress one normally — only reruns are held back from cancelling.
  cancel-in-progress: ${{ github.run_attempt == 1 }}

jobs:
  wait-for-codex:
    name: Wait for Codex Review
    # dependabot[bot] excluded outright, not just from the re-review-request
    # step: GitHub withholds Actions secrets (and downgrades GITHUB_TOKEN) for
    # Dependabot-triggered events, so CODEX_REVIEW_PAT is empty on every
    # Dependabot push — but the poll step below has no way to see that and
    # would still wait out the full 75-minute timeout for a Codex response
    # that structurally cannot arrive, failing every weekly Dependabot PR
    # this repo's own dependabot-auto-merge workflow is supposed to handle
    # (Codex P2, flagged reviewing this file). A skipped required check
    # satisfies branch protection the same as the existing draft-PR no-op.
    #
    # github.actor (not pull_request.user.login) is the deliberate test: a
    # maintainer manually pushing to a Dependabot-authored branch makes THAT
    # actor the triggering identity for the run, and GitHub's secret
    # withholding is keyed to the triggering actor, not the PR's persistent
    # author field — so secrets ARE available on that push, and gating on
    # the PR's original author would wrongly skip Codex review for it
    # (considered and rejected reviewing this file — the suggested
    # pull_request.user.login alternative would be a regression, not a fix).
    #
    # Consolidated into ONE job with event-gated steps, not two jobs
    # sharing this check name (the earlier split): GitHub still creates a
    # check-run for a job whose `if:` evaluates false and reports it
    # "skipped" within seconds — with two same-named check-runs per run,
    # relying on undocumented same-name multi-run matching semantics for a
    # REQUIRED check is exactly the kind of ambiguity worth avoiding
    # outright rather than trusting (Codex P1, flagged reviewing this
    # file). One job publishing exactly one check-run per run removes the
    # ambiguity entirely.
    if: github.event_name == 'merge_group' || (github.event.pull_request.draft == false && github.actor != 'dependabot[bot]')
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - name: Pass through for merge-queue evaluation
        if: github.event_name == 'merge_group'
        run: |
          echo "Merge-group evaluation — the constituent PR already passed a real Wait for Codex Review during its own PR phase; nothing further to review on this synthetic integration commit."

      - name: Record run start
        if: github.event_name == 'pull_request'
        env:
          # Seed from the PR event's OWN updated_at (set by GitHub at dispatch
          # time), not a runner-observed "now". A queued or slow-starting
          # runner previously computed its cutoff only after the runner was
          # already up, so a Codex response that landed in the gap between
          # the real event and the runner actually starting was silently
          # older than the cutoff and got ignored as "before this round"
          # (Codex P2, flagged reviewing this file). Doing this as its own
          # unconditional first step means every trigger type (not just
          # synchronize) gets the same immune-to-runner-startup timestamp.
          PR_UPDATED_AT: ${{ github.event.pull_request.updated_at }}
        run: |
          RUN_STARTED=$(date -u -d "$PR_UPDATED_AT - 15 seconds" +%Y-%m-%dT%H:%M:%SZ)
          echo "$RUN_STARTED" > /tmp/codex-run-started.txt

      - name: Skip if this run's head no longer matches the PR's current head
        id: head-check
        if: github.event_name == 'pull_request'
        # ACCEPTED LIMITATION, not fixed here: this step no-ops a stale
        # rerun's OWN remaining steps, but concurrency (`cancel-in-progress`
        # keyed on PR number) is evaluated by GitHub before any step runs —
        # so if a human manually reruns an old attempt while the current
        # head's run is still executing, that stale rerun starting can
        # itself cancel the run actually gating the current head, and
        # nothing here recreates it (Codex P2, flagged reviewing this
        # file). Keying concurrency by head SHA instead of PR number would
        # fix this narrow case but reopen the exact problem concurrency
        # exists to prevent (a superseded PUSH's run no longer getting
        # cancelled by the next push, since each push already has its own
        # SHA). This failure mode is narrow (requires a human to
        # deliberately rerun a stale, non-current attempt) and fails SAFE,
        # not open: the required check ends up cancelled/pending, which
        # blocks merge — it never lets a stale result satisfy the gate. A
        # human noticing and rerunning the CURRENT head's own job (or just
        # pushing again) recovers it.
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
          EVENT_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          # A manual rerun of an old attempt replays the ORIGINAL event
          # payload, including its now-stale head SHA — the PR-scoped
          # concurrency group can let that stale rerun cancel the run
          # actually gating the current head, and if it proceeds anyway it
          # reconstructs the same stale cutoff and rediscovers the same old
          # signal/error rather than gating the commit that's actually
          # about to merge (Codex P2, flagged reviewing this file). Compare
          # against the PR's LIVE head at run time and no-op if this run is
          # for a superseded commit — a fresh run already exists (or will
          # exist) for the real current head and is the one that should gate.
          # Bounded retry, not a bare assignment: a transient REST/auth/
          # rate-limit failure here would otherwise abort the whole required
          # check before polling even begins, under this step's default
          # `bash -e` (Codex P2, flagged reviewing this file). 3 attempts
          # over ~15s is enough to absorb a genuine blip; if it's still
          # failing after that, proceed as if not stale rather than crash —
          # the downstream checks (reviewed_head, new_count, per-thread live
          # rechecks) are the real safety net regardless of this step's
          # own verdict.
          CURRENT_HEAD_SHA=""
          for _ in 1 2 3; do
            CURRENT_HEAD_SHA=$(gh api "repos/$REPO/pulls/$PR" --jq '.head.sha' 2>/tmp/head-lookup-err.log) && break
            echo "::warning::Transient error looking up the PR's current head — retrying in 5s: $(cat /tmp/head-lookup-err.log)"
            sleep 5
          done
          if [ -z "$CURRENT_HEAD_SHA" ]; then
            echo "::warning::Could not confirm the PR's current head after 3 attempts — proceeding as if this run's head is current."
            CURRENT_HEAD_SHA="$EVENT_HEAD_SHA"
          fi
          if [ "$EVENT_HEAD_SHA" != "$CURRENT_HEAD_SHA" ]; then
            echo "::notice::This run's head ($EVENT_HEAD_SHA) no longer matches the PR's current head ($CURRENT_HEAD_SHA) — skipping as superseded."
            echo "stale=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Request re-review on a re-push
        # Also fires on reopened (see the trigger-level comment above) and
        # on any rerun (run_attempt > 1), not just synchronize: a
        # maintainer rerunning a failed opened/ready_for_review attempt
        # (e.g. after a transient Codex quota/auth error) reuses the SAME
        # event and, without this, would reconstruct the same stale cutoff
        # and rediscover the identical old error forever rather than asking
        # Codex to look again (Codex P2, flagged reviewing this file).
        if: github.event_name == 'pull_request' && steps.head-check.outputs.stale != 'true' && (github.event.action == 'synchronize' || github.event.action == 'reopened' || github.run_attempt > 1)
        env:
          # Codex only auto-reviews on opened/ready_for_review, not on every
          # push (this repo's own AGENTS.md: "not every push. After a
          # remediation push, request a re-review with `@codex review`" — and
          # that trigger requires an authenticated human comment; a
          # github-actions[bot]-authored comment gets "create a Codex account
          # and connect to github" instead of a review, confirmed live on
          # HagaleTechnologies/harbor#56). CODEX_REVIEW_PAT is a fine-grained
          # PAT under an account Codex already recognizes, stored as an org
          # secret, used ONLY to post this one comment.
          GH_TOKEN: ${{ secrets.CODEX_REVIEW_PAT }}
          # Read-only, always fully scoped (unlike a fine-grained PAT, which
          # this step's own docs say is provisioned ONLY for posting the
          # comment below) — used for the baseline review-threads read so a
          # minimally-scoped PAT lacking PR-read can't crash this step before
          # the request is even posted (Codex P2, flagged reviewing this
          # file).
          READ_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          set -o pipefail
          # GitHub does not expose secrets to a `pull_request`-triggered
          # workflow run for a fork PR (by design — see GitHub's own docs on
          # securely using pull_request_target/pull_request with forks), so
          # CODEX_REVIEW_PAT expands to empty here and every `gh api` call
          # below would fail auth and crash this step under `bash -e`,
          # blocking a fork contributor's re-push behind a hard failure
          # instead of just skipping the re-review request (Codex P1,
          # flagged reviewing this file).
          #
          # DELIBERATELY NOT auto-discovering a maintainer's manual `@codex
          # review` comment here: an earlier version of this fix tried that
          # (a REST lookup + node_id capture) and Codex's review of THAT fix
          # found three more real gaps in it — unpaginated (misses anything
          # past GitHub's default 30-comment page), one-shot (a comment added
          # during the 70-minute wait is never seen), and its knock-on effect
          # on the error-comment check below. Chasing that kept spawning new
          # edge cases in a path this repo is unlikely to ever exercise (a
          # private repo with a single admin taking no outside fork PRs
          # today). Accepted, deliberate scope limit instead: on a fork
          # re-push, the poll step still catches a formal review tied to
          # HEAD_SHA with zero extra code (that path never needed a captured
          # comment id). The one case this doesn't cover — Codex's clean-pass
          # landing ONLY as a reaction on a maintainer's own manually-posted
          # comment, never a formal review — degrades to the 70-minute
          # timeout, at which point the repo's admin-only bypass (configured
          # on every HagaleTechnologies ruleset) is the intended way through,
          # not more automation for a rare path.
          if [ -z "$GH_TOKEN" ]; then
            echo "CODEX_REVIEW_PAT not available (fork PR, or secret unset) — skipping the fresh re-review request. A formal Codex review tied to this head still resolves the check normally; a maintainer can otherwise use the repo's admin bypass."
            exit 0
          fi

          OWNER="${REPO%%/*}"
          NAME="${REPO##*/}"

          # Snapshot unresolved Codex-*started* review threads BEFORE asking
          # for a fresh look. Only a thread with EVERY comment authored by
          # Codex (or this workflow's own prior auto-resolve reply, which
          # carries no new human concern) is eligible for later
          # auto-resolution — a thread any human has ever replied in is
          # excluded permanently, not just for this round, so a clean Codex
          # re-review can never silently override a human's still-open
          # concern (flagged reviewing this file, on harbor#60; comments
          # (first: 50) instead of first: 1 for this). Treating this
          # workflow's own reply as ignorable closes a separate gap: if a
          # prior round's reply succeeded but resolveReviewThread then failed
          # (permissions/transient), that thread must stay eligible for retry
          # on the next clean re-review rather than becoming permanently
          # manual just because it now contains a non-Codex comment (Codex
          # P3, this file's own auto-merge rollout).
          #
          # KNOWN LIMITATION, accepted not fixed: reviewThreads(first: 100)
          # doesn't paginate past 100 threads, and comments(first: 50) doesn't
          # paginate past 50 replies in one thread (both flagged on harbor#59
          # /#60). Neither is realistic on these repos; revisit if that ever
          # stops being true.
          #
          # NOTE on the author check below: "github-actions" (no `[bot]`
          # suffix) is deliberate, not a REST-vs-GraphQL mixup — verified
          # empirically against this exact rollout's own data (2026-08-05):
          # GraphQL's `author.login` never carries the `[bot]` suffix REST
          # uses (`dependabot`, not `dependabot[bot]`, appears in this same
          # repo's own review-thread comments), matching `chatgpt-codex-
          # connector` elsewhere in this file. A later Codex pass suggested
          # adding the `[bot]`-suffixed form here on the theory that it's
          # what a GITHUB_TOKEN-authored reply's login looks like — that
          # variant does not occur in GraphQL responses, so it was not added.
          baseline_query='
            query($owner: String!, $name: String!, $pr: Int!) {
              repository(owner: $owner, name: $name) {
                pullRequest(number: $pr) {
                  reviewThreads(first: 100) {
                    nodes {
                      id
                      isResolved
                      comments(first: 50) { nodes { author { login } createdAt } }
                    }
                  }
                }
              }
            }'
          baseline_response=$(GH_TOKEN="$READ_TOKEN" gh api graphql -f query="$baseline_query" -f owner="$OWNER" -f name="$NAME" -F pr="$PR")
          echo "$baseline_response" \
            | jq '[.data.repository.pullRequest.reviewThreads.nodes[]
              | select(.isResolved == false)
              | select([.comments.nodes[].author.login] | all(. == "chatgpt-codex-connector" or . == "github-actions"))
              | .id]' \
            > /tmp/codex-baseline-threads.json

          # Second, BROADER baseline for deciding what's "new" this round
          # (as opposed to /tmp/codex-baseline-threads.json above, which
          # stays purity-filtered and is only for deciding what's safe to
          # auto-resolve): a thread Codex started before this round, that
          # since got a human reply, still isn't a NEW concern — but the
          # poll step's new_count check now identifies Codex-started threads
          # by their FIRST comment only (see that step's comments for why),
          # so without this second snapshot such a thread would look "new"
          # forever (never in the strict pure-only baseline, since the human
          # reply excludes it there), permanently blocking every future
          # round's auto-resolution (Codex P2, flagged reviewing this file —
          # a direct follow-up to the codex_initiated fix in the poll step).
          echo "$baseline_response" \
            | jq '[.data.repository.pullRequest.reviewThreads.nodes[]
              | select(.isResolved == false)
              | select(.comments.nodes[0].author.login == "chatgpt-codex-connector")
              | .id]' \
            > /tmp/codex-baseline-initiated.json

          request_comment=$(gh api "repos/$REPO/issues/$PR/comments" -f body="@codex review")

          # Capture the comment's own GraphQL node_id (not its REST numeric
          # id — the poll step's combined query looks it up via GraphQL's
          # `node(id:)`, which needs the node_id): a clean re-review can land
          # as a 👍 reaction on THIS comment specifically, not on the PR/
          # issue itself (issue-level and comment-level reactions are
          # separate endpoints — flagged reviewing this file, on harbor#59).
          echo "$request_comment" | jq -r '.node_id' > /tmp/codex-review-comment-id.json

          # Re-anchor RUN_STARTED to the moment THIS request was actually
          # posted, not the broader PR_UPDATED_AT snapshot from "Record run
          # start": a rerun of an already-completed synchronize job (or, as
          # observed live on pancetta#239, several small pushes to the same
          # PR in quick succession) can leave RUN_STARTED older than a
          # genuinely fresh re-review request, letting an already-baselined
          # prior review of the same HEAD_SHA satisfy the poll without ever
          # waiting for Codex to look again (Codex P1, flagged reviewing this
          # file). This request's own timestamp is always the correct,
          # tightest cutoff for a synchronize round.
          echo "$request_comment" | jq -r '.created_at' > /tmp/codex-run-started.txt

      - name: Poll for a terminal Codex signal on this revision
        if: github.event_name == 'pull_request' && steps.head-check.outputs.stale != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Only for the reply+resolve mutations at the end of this step —
          # GITHUB_TOKEN (the github-actions[bot] identity) reliably fails
          # resolveReviewThread on Codex-started threads with "Resource not
          # accessible by integration" (confirmed live on pancetta#239: 10/10
          # resolve attempts failed this way, replies posted fine). A PAT
          # under a real user account with actual write access resolves the
          # same threads without issue — proven directly this session
          # resolving all 22 threads on coppa#77 by hand under this exact
          # identity. Reusing CODEX_REVIEW_PAT rather than provisioning a
          # second secret, since it's already a real-user PAT with repo
          # write access, just used for a second purpose now.
          RESOLVE_TOKEN: ${{ secrets.CODEX_REVIEW_PAT }}
          REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          # reopened and a manual rerun (run_attempt>1) both post a fresh
          # @codex review request (see the step above) — IS_SYNC drives the
          # poll's re-review-vs-initial-review branch downstream, so it must
          # recognize the same three cases the request step does, or a
          # reopened/rerun round wrongly follows the initial-review branch:
          # an unrelated fresh comment/reaction could satisfy the check
          # without being tied to the new head, and a genuine clean
          # re-review could fail on baselined threads it should auto-resolve
          # instead (Codex P1, flagged reviewing this file — the reopened
          # case; run_attempt extended proactively to close the identical
          # gap there before it needed its own separate finding).
          IS_SYNC: ${{ github.event.action == 'synchronize' || github.event.action == 'reopened' || github.run_attempt > 1 }}
          # Narrower than IS_SYNC on purpose: a reopened event or a rerun of
          # opened/ready_for_review has no new commit, so a clean re-review
          # of the SAME code must not auto-resolve baseline threads still
          # describing that same code — only a genuine synchronize proves
          # the head actually changed (Codex P1, flagged reviewing this
          # file). IS_SYNC stays broadened for poll-branch selection (which
          # signal shape counts as "responded"); this only gates whether
          # auto-resolution is allowed to run at all.
          # run_attempt == 1 required too, not just action == synchronize:
          # GitHub replays the ORIGINAL event payload on a manual rerun, so
          # a rerun of a synchronize attempt still reports action ==
          # 'synchronize' even though no new commit landed — the same
          # no-new-commit case already excluded for opened/ready_for_review
          # reruns, just reachable through a different event type (Codex
          # P1, flagged reviewing this file).
          IS_GENUINE_SYNC: ${{ github.event.action == 'synchronize' && github.run_attempt == 1 }}
          # Deliberately NARROWER than IS_SYNC: run_attempt>1 alone means a
          # maintainer clicked "re-run" on the SAME head with no new commit
          # — the baseline snapshot IS the original unresolved findings
          # themselves, so a clean re-review with zero NEW findings does not
          # mean those findings were fixed, just that Codex didn't find
          # anything ADDITIONAL. Auto-resolving on that basis would silently
          # clear real, still-open findings with zero remediation ever
          # happening (Codex P1, flagged reviewing this file on
          # widdershins#114). Only a genuine new commit (synchronize/
          # reopened) is safe to treat as a remediation round.
          # `reopened` removed from this condition (fresh evidence beyond
          # the run_attempt exclusion above): a PR can be closed and
          # reopened with ZERO intervening commits — same head SHA, same
          # unresolved findings — and reopened alone carries no guarantee
          # the head actually changed, unlike `synchronize`, which by
          # GitHub's own definition of that action type always means a new
          # commit landed. Treating a no-op reopen as remediation has the
          # identical unsafe-auto-resolve failure mode as run_attempt>1
          # above (Codex P1, flagged reviewing this file on
          # widdershins#114, directly extending that same finding). The
          # trade-off is deliberately asymmetric: a genuine reopen-with-new-
          # commits case now needs a human to notice and re-trigger, rather
          # than risk silently clearing a real unaddressed finding.
          IS_REMEDIATION: ${{ github.event.action == 'synchronize' }}
        run: |
          set -o pipefail
          # Only count signals created at/after this run started, and reviews
          # submitted against the CURRENT head commit AND after this round
          # started (a small buffer absorbs clock skew between this runner
          # and Codex; the timestamp bound on reviews specifically closes the
          # stale-review gap noted in the step above).
          #
          # A single combined GraphQL query per poll, not 3-4 separate REST
          # calls: `GITHUB_TOKEN` is capped at 1,000 requests/hour/repository
          # (shared across every concurrent workflow run in the repo, not
          # just this one), and the previous 4-calls-per-20s loop could drive
          # 1,000+ req/hr on its own with just two concurrent PRs under
          # review — enough to make this required check fail on a rate limit
          # rather than a real Codex signal (Codex P2, flagged reviewing this
          # file, this rollout's own auto-merge PR).
          OWNER="${REPO%%/*}"
          NAME="${REPO##*/}"
          RUN_STARTED=$(cat /tmp/codex-run-started.txt 2>/dev/null || date -u -d '15 seconds ago' +%Y-%m-%dT%H:%M:%SZ)
          # Bumped from 18min -> 28min -> 70min over this rollout: burst load
          # (many PRs/pushes landing across the org at once) has pushed
          # Codex's real response time past 45-50 minutes on at least one
          # occasion. This margin is deliberately generous, not arbitrary.
          END=$((SECONDS+4200))
          responded=""
          reviewed_head=""
          COMMENT_ID=$(cat /tmp/codex-review-comment-id.json 2>/dev/null || echo "")
          HAS_COMMENT="false"
          [ -n "$COMMENT_ID" ] && HAS_COMMENT="true"
          while [ $SECONDS -lt $END ]; do
            # A transient GraphQL read-after-write gap can make node($commentId)
            # briefly 404 right after the comment was posted via REST — this
            # loop's own retry cadence is the right place to absorb that, not
            # a hard crash. Guarding with `if !` (rather than a bare
            # assignment) is required under this step's default `bash -e`:
            # an unguarded failing command here kills the whole step instantly
            # instead of reaching the next iteration (observed live on
            # skimmer#45: the very first poll hit "Could not resolve to a
            # node" for a comment created less than a second earlier, and the
            # required check failed in 6 seconds instead of retrying).
            if ! gh_poll_raw=$(gh api graphql -f query='
              query($owner: String!, $name: String!, $pr: Int!, $hasComment: Boolean!, $commentId: ID!) {
                repository(owner: $owner, name: $name) {
                  pullRequest(number: $pr) {
                    headRefOid
                    comments(last: 50) { nodes { author { login } body createdAt } }
                    reviews(last: 50) { nodes { author { login } commit { oid } submittedAt } }
                    reactions(last: 50, content: THUMBS_UP) { nodes { user { login } createdAt } }
                  }
                }
                commentNode: node(id: $commentId) @include(if: $hasComment) {
                  ... on IssueComment {
                    reactions(last: 50, content: THUMBS_UP) { nodes { user { login } createdAt } }
                  }
                }
              }' -f owner="$OWNER" -f name="$NAME" -F pr="$PR" -F hasComment="$HAS_COMMENT" -f commentId="${COMMENT_ID:-placeholder}" 2>/tmp/poll-err.log); then
              echo "::warning::Transient error polling for a Codex signal — retrying in 20s. $(cat /tmp/poll-err.log 2>/dev/null)"
              sleep 20
              continue
            fi
            # gh's own failure is now checked directly above (if !), not
            # swallowed by || true: a failed call previously fell through as
            # empty $gh_poll_raw, and jq treats fully-empty input as a
            # zero-value stream and exits 0 by default — the retry branch
            # never fired, and every counter below silently evaluated
            # empty/unset for the rest of the 70-minute budget instead of
            # retrying (Codex P2, flagged reviewing this file on
            # watchfinder#33, round 3). headRefOid is folded into this same
            # query rather than a separate per-iteration REST call — with
            # several PRs waiting concurrently, a standalone REST head-check
            # plus this poll every 20s pushed close to the shared 1,000/hr/
            # repository GITHUB_TOKEN budget this file documents above
            # (Codex P2, same review round).
            #
            # Head-SHA-keyed concurrency (see the group: comment above) no
            # longer auto-cancels an older run when a newer push lands, since
            # each head now gets its own group — so without this check, a
            # run for a superseded head could keep polling for up to 70
            # minutes and eventually execute the thread-resolution mutations
            # below using a review tied to a commit that's no longer HEAD
            # (Codex P2, flagged reviewing this file on skimmer's sync PR).
            # Re-checked every iteration (cheap next to the poll query
            # itself), not just once before the loop starts — a push landing
            # mid-poll must be caught here too, not just at step entry.
            CURRENT_HEAD_SHA=$(echo "$gh_poll_raw" | jq -r '.data.repository.pullRequest.headRefOid // empty')
            if [ -n "$CURRENT_HEAD_SHA" ] && [ "$CURRENT_HEAD_SHA" != "$HEAD_SHA" ]; then
              echo "::notice::A newer push has superseded this run's head ($HEAD_SHA) — a fresh run now gates $CURRENT_HEAD_SHA instead. Exiting cleanly without touching review threads."
              exit 0
            fi
            if ! poll=$(echo "$gh_poll_raw" | jq --arg since "$RUN_STARTED" --arg sha "$HEAD_SHA" '
                def is_error_shaped: test("(you exceeded your current quota|quota exceeded|rate limit reached|rate.?limit exceeded|unable to (access|start|review)|cannot access|authentication (failed|error)|workspace error|failed to (access|clone|start)|create a codex account and connect)"; "i");
                # Codex'"'"'s clean-pass response on a re-push can ALSO land as a
                # plain issue comment (not a formal review, not a reaction) whose
                # body embeds "**Reviewed commit:** `<10-hex-char prefix>`" — a
                # real, empirically observed shape (confirmed live on this
                # rollout: HagaleTechnologies/pancetta#239, Codex replied
                # "Didn'"'"'t find any major issues... Reviewed commit: `7ed84e468d`"
                # matching that PR'"'"'s actual head SHA, and the required check
                # still spun for 44+ minutes because neither `reviews` nor
                # `comment_thumbsup` ever fires for this shape). This IS a
                # genuinely head-tied signal, just carried in the comment text
                # instead of a separate GraphQL field.
                def reviewed_commit_prefix: try (.body | capture("[Rr]eviewed commit:?\\**\\s*`(?<c>[0-9a-f]{7,40})`").c) catch null;
                .data.repository.pullRequest as $p
                | ([$p.comments.nodes[] | select(.author.login=="chatgpt-codex-connector" and .createdAt >= $since)]) as $fresh_comments
                | {
                    comments: ([$fresh_comments[] | select(.body | is_error_shaped | not)] | length),
                    comment_head_tied: ([$fresh_comments[]
                      | select(.body | is_error_shaped | not)
                      | select((reviewed_commit_prefix // "") as $c | $c != "" and ($sha | startswith($c)))
                    ] | length),
                    error_comments: ([$fresh_comments[] | select(.body | is_error_shaped)] | length),
                    reviews: ([$p.reviews.nodes[] | select(.author.login=="chatgpt-codex-connector" and .commit.oid==$sha and .submittedAt >= $since)] | length),
                    thumbsup: ([$p.reactions.nodes[] | select(.user.login=="chatgpt-codex-connector" and .createdAt >= $since)] | length),
                    comment_thumbsup: ([(.data.commentNode.reactions.nodes // [])[] | select(.user.login=="chatgpt-codex-connector" and .createdAt >= $since)] | length)
                  }'); then
              # The read-after-write shape (node($commentId) briefly
              # 404ing right after the comment was posted) plus genuine
              # transport/server hiccups (5xx, timeout, connection reset —
              # gh's own wording for these varies: "HTTP 5xx", "context
              # deadline exceeded", "connection reset", "EOF", "i/o
              # timeout") are the only classes known to self-resolve by
              # waiting; anything else (bad/revoked token, invalid query, a
              # genuine permanent API error) never will, so it fails fast
              # instead of retrying blind for the full 70-minute window
              # (Codex P2, flagged reviewing this file on widdershins#114 and,
              # on this exact classification, again on sunto's sync PR —
              # the original single-phrase match missed ordinary transient
              # transport/server errors entirely).
              if grep -qiE "could not resolve to a node|HTTP 5[0-9]{2}|context deadline exceeded|connection reset|timeout|\bEOF\b" /tmp/poll-err.log 2>/dev/null; then
                echo "::warning::Transient error polling for a Codex signal — retrying in 20s. $(cat /tmp/poll-err.log 2>/dev/null)"
                sleep 20
                continue
              fi
              echo "::error::Non-transient error polling for a Codex signal — failing fast: $(cat /tmp/poll-err.log 2>/dev/null)"
              exit 1
            fi
            comments=$(echo "$poll" | jq '.comments')
            comment_head_tied=$(echo "$poll" | jq '.comment_head_tied')
            error_comments=$(echo "$poll" | jq '.error_comments')
            reviews=$(echo "$poll" | jq '.reviews')
            thumbsup=$(echo "$poll" | jq '.thumbsup')
            comment_thumbsup=$(echo "$poll" | jq '.comment_thumbsup')
            if [ "$reviews" -gt 0 ] || [ "$comment_thumbsup" -gt 0 ] || [ "$comment_head_tied" -gt 0 ]; then
              # A 👍 on the @codex-review comment itself, or a plain comment
              # whose own text names THIS head's commit, are just as tightly
              # scoped to "responded to THIS request" as a formal review tied
              # to HEAD_SHA — Codex's clean-pass signal is often only one of
              # these, never a formal review object (flagged reviewing this
              # file, on harbor#59; the comment-text shape found live on
              # pancetta#239, see above).
              reviewed_head=1
            fi
            if [ "$IS_SYNC" = "true" ]; then
              # On a re-push specifically, a bare new issue comment or a
              # generic issue-level thumbsup is NOT proof Codex looked at
              # THIS head — either could be a stale signal from an unrelated
              # earlier review landing after RUN_STARTED but before the fresh
              # `@codex review` request was actually processed, which let
              # this required check pass without any head-tied Codex signal
              # ever arriving (Codex P1, flagged reviewing this file). Only
              # accept signals that are provably tied to this specific
              # request: a review against HEAD_SHA, a reaction on the comment
              # that requested it, or a comment whose own text names this
              # HEAD_SHA. `error_comments` is deliberately NOT checked on this
              # branch — it exists only to keep a bare error comment from
              # satisfying the non-sync "any comment counts" condition below,
              # and an unscoped error from some earlier, unrelated request
              # must not fail the CURRENT push's own still-pending signal
              # (Codex P2, flagged reviewing this file).
              if [ "$reviews" -gt 0 ] || [ "$comment_thumbsup" -gt 0 ] || [ "$comment_head_tied" -gt 0 ]; then
                echo "Codex responded on this revision: reviews=$reviews comment_thumbsup=$comment_thumbsup comment_head_tied=$comment_head_tied"
                responded=1
                break
              fi
            else
              # Check for a genuine success FIRST, error fail-fast second: a
              # manually re-run job reuses the original event's cutoff, so an
              # older transient Codex error and a later real success can both
              # be in-window on the same poll. Only fail fast on the error
              # when there's no success signal alongside it — otherwise a
              # rerun (which this exact error message tells maintainers to
              # do) could stay permanently poisoned by the first attempt's
              # failure even after Codex succeeds on the second (Codex P2,
              # flagged reviewing this file).
              if [ "$comments" -gt 0 ] || [ "$reviews" -gt 0 ] || [ "$thumbsup" -gt 0 ] || [ "$comment_thumbsup" -gt 0 ]; then
                echo "Codex responded on this revision: comments=$comments reviews=$reviews thumbsup=$thumbsup comment_thumbsup=$comment_thumbsup"
                responded=1
                break
              fi
              if [ "$error_comments" -gt 0 ]; then
                # Codex replies with an issue comment for its own failures too
                # (auth/workspace/quota/account-connection errors), which
                # would otherwise satisfy the "any comment counts" condition
                # above and turn this required check green with no actual
                # review ever happening (Codex P2, flagged reviewing this
                # file). Only reachable here (never on a re-push, see above)
                # since this is the one branch that treats a bare comment as
                # a success signal at all. Fail loudly and immediately rather
                # than either accepting it as success OR silently waiting out
                # the remaining timeout for a different signal — Tony's own
                # directive: don't waste wall-clock time on a wait that a
                # Codex-reported error already tells us won't resolve itself.
                #
                # KNOWN LIMITATION, accepted not fixed: a fresh, genuinely
                # unrelated Codex issue comment (e.g. someone asks
                # `@codex fix CI` while this gate is waiting) would also
                # satisfy the "any comment counts" condition above and pass
                # as if it were a real review — this branch only guards
                # against Codex's own documented error phrases, not against
                # positively confirming a comment IS a completed review.
                # Building a reliable positive classifier for "this comment
                # proves a real review happened" is real ongoing work for
                # marginal benefit on a low-traffic gate; revisit if an
                # unrelated-comment false-pass is ever actually observed
                # (flagged reviewing this file).
                echo "::error::Codex reported an error (quota/auth/workspace/account-connection) instead of reviewing $HEAD_SHA — failing now rather than waiting out the remaining timeout. Re-run this check once the underlying Codex issue is resolved."
                exit 1
              fi
            fi
            echo "Still waiting for Codex to respond to $HEAD_SHA..."
            sleep 20
          done
          if [ -z "$responded" ]; then
            echo "::error::Codex did not respond to $HEAD_SHA within 70 minutes"
            exit 1
          fi

          if [ "$IS_REMEDIATION" != "true" ]; then
            # This branch previously exited 0 the moment ANY Codex signal was
            # seen, without checking whether that signal actually left
            # unresolved concerns open — so Codex's own initial automatic
            # review finding real P1s could still turn this check green
            # (Codex P2, flagged reviewing this file). Not a live merge-
            # safety hole on this fleet specifically (every HagaleTechnologies
            # ruleset also sets required_review_thread_resolution, which
            # independently blocks merge regardless of this check's status —
            # confirmed empirically on coppa#77), but this check's own name
            # and green/red status should mean what it says rather than
            # relying on a separate, easily-forgotten ruleset setting to
            # catch the gap. Query and fail on any unresolved Codex-STARTED
            # thread before declaring success — tested by the thread's FIRST
            # comment (codex_initiated), not "every comment is Codex", so a
            # maintainer replying to a brand-new Codex finding before this
            # query runs can't hide it (the same gap the synchronize path's
            # new_count check had, flagged reviewing this file as a direct
            # follow-up — the initial-review path had it too and hadn't been
            # updated yet).
            OWNER="${REPO%%/*}"
            NAME="${REPO##*/}"
            unresolved=$(gh api graphql -f query='
              query($owner: String!, $name: String!, $pr: Int!) {
                repository(owner: $owner, name: $name) {
                  pullRequest(number: $pr) {
                    reviewThreads(first: 100) {
                      nodes { isResolved comments(first: 50) { nodes { author { login } } } }
                    }
                  }
                }
              }' -f owner="$OWNER" -f name="$NAME" -F pr="$PR" \
              | jq '[.data.repository.pullRequest.reviewThreads.nodes[]
                | select(.isResolved == false)
                | select(.comments.nodes[0].author.login == "chatgpt-codex-connector")
              ] | length')
            if [ "$unresolved" -gt 0 ]; then
              echo "::error::Codex's initial review left $unresolved unresolved concern(s) open — resolve them, then push a remediation commit to trigger a fresh re-review."
              exit 1
            fi
            exit 0
          fi

          # Auto-resolve stale threads ONLY on a genuine head-tied review (not
          # just any comment/reaction, which could be an unrelated status
          # message rather than Codex actually finishing a look at this
          # revision — flagged reviewing this file, on harbor#59).
          if [ -z "$reviewed_head" ]; then
            echo "Codex responded, but not with a review tied to $HEAD_SHA specifically — not auto-resolving anything."
            exit 0
          fi

          if [ "$IS_GENUINE_SYNC" != "true" ]; then
            echo "This round is a reopened/rerun, not a genuine synchronize — the code hasn't changed, so not auto-resolving baseline threads that may still describe it."
            exit 0
          fi

          OWNER="${REPO%%/*}"
          NAME="${REPO##*/}"
          baseline=$(cat /tmp/codex-baseline-threads.json 2>/dev/null || echo '[]')
          baseline_initiated=$(cat /tmp/codex-baseline-initiated.json 2>/dev/null || echo '[]')

          # Re-fetch full thread state and compute, in one pass:
          #   new_count  — unresolved, PURE-CODEX threads NOT in baseline
          #                (Codex raised something brand new this round).
          #                Deliberately scoped to Codex-authored threads only
          #                — an unrelated pre-existing human thread (or a
          #                Codex thread a human already joined) must not
          #                block auto-resolving OTHER, still-clean baseline
          #                threads just by being unresolved (Codex P3,
          #                flagged reviewing this file).
          #   resolvable — baseline threads that are STILL unresolved (a
          #                maintainer may have resolved one by hand while
          #                this job was waiting — checked again now, not
          #                assumed from the snapshot) AND STILL pure-Codex/
          #                workflow-reply-only (no other human joined since
          #                the snapshot) AND received no fresh Codex comment
          #                after RUN_STARTED (Codex restating a concern IN an
          #                existing thread, rather than opening a new one,
          #                must not count as "clean" — all three gaps
          #                flagged reviewing this file, on harbor#60 and this
          #                file's own auto-merge rollout).
          result=$(gh api graphql -f query='
            query($owner: String!, $name: String!, $pr: Int!) {
              repository(owner: $owner, name: $name) {
                pullRequest(number: $pr) {
                  reviewThreads(first: 100) {
                    nodes {
                      id
                      isResolved
                      comments(first: 50) { nodes { author { login } createdAt } }
                    }
                  }
                }
              }
            }' -f owner="$OWNER" -f name="$NAME" -F pr="$PR" \
            | jq --argjson baseline "$baseline" --argjson baselineInitiated "$baseline_initiated" --arg since "$RUN_STARTED" '
              def ignorable_author: . == "chatgpt-codex-connector" or . == "github-actions";
              # "New concern" and "safe to auto-resolve" need DIFFERENT
              # authorship tests, not the same purity filter (Codex P2,
              # flagged reviewing this file): if Codex opens a genuinely new
              # thread this round and a maintainer replies to it before this
              # query runs, the OLD single "all comments ignorable" test
              # dropped that thread from new_count entirely, silently letting
              # the round auto-resolve every OTHER baseline thread despite an
              # unaddressed live concern. codex_initiated (did CODEX START
              # this thread, regardless of who replied since) is the right
              # test for "is this a concern Codex raised" — used for
              # new_count, compared against $baselineInitiated (the SAME
              # codex_initiated test snapshotted before this round — NOT
              # $baseline, which is purity-filtered: an old Codex-started
              # thread that already had a human reply BEFORE this round would
              # never be in the strict $baseline, so diffing against it would
              # make that old thread look "new" forever, permanently blocking
              # every future round of auto-resolution — a direct follow-up gap
              # flagged reviewing this file). ignorable_author on EVERY
              # comment (unchanged, stricter) stays the test for "is this
              # safe to silently auto-resolve", diffed against the strict
              # $baseline as before — a human joining a thread, new or old,
              # should always take it out of auto-resolution, whether or not
              # it also counts as a blocking new concern.
              def codex_initiated: (.comments.nodes[0].author.login == "chatgpt-codex-connector");
              .data.repository.pullRequest.reviewThreads.nodes as $nodes
              | ($nodes
                  | map(select(.isResolved == false)
                        | select(codex_initiated)
                        | .id)) as $codex_unresolved
              | {
                  new_count: (($codex_unresolved - $baselineInitiated) | length),
                  resolvable: [
                    $nodes[]
                    | select(.isResolved == false)
                    | select(.id as $id | $baseline | index($id))
                    | select([.comments.nodes[].author.login] | all(ignorable_author))
                    | select([.comments.nodes[] | select(.createdAt >= $since)] | length == 0)
                    | .id
                  ]
                }')

          new_count=$(echo "$result" | jq '.new_count')
          if [ "$new_count" -gt 0 ]; then
            echo "Codex raised $new_count new concern(s) on this revision — leaving all threads as-is for human review."
            exit 0
          fi

          echo "$result" | jq -r '.resolvable[]' | while read -r thread_id; do
            [ -z "$thread_id" ] && continue
            # Re-check PR-WIDE for a new Codex concern before touching THIS
            # thread — the $new_count check above ran once before this loop
            # started, but Codex can open a brand-new thread while this loop
            # is still walking earlier baseline threads (each iteration costs
            # 2-3 network round-trips), and a thread-scoped live recheck can
            # never see a concern that landed in a DIFFERENT thread (Codex
            # P2, flagged reviewing this file). Bails the whole loop, not
            # just this thread — a fresh concern means "leave everything for
            # human review" exactly as the pre-loop new_count check already
            # means.
            if ! fresh_check=$(gh api graphql -f query='
              query($owner: String!, $name: String!, $pr: Int!) {
                repository(owner: $owner, name: $name) {
                  pullRequest(number: $pr) {
                    reviewThreads(first: 100) {
                      nodes { isResolved comments(first: 1) { nodes { author { login } } } }
                    }
                  }
                }
              }' -f owner="$OWNER" -f name="$NAME" -F pr="$PR" 2>/tmp/fresh-check-err.log \
              | jq --argjson baselineInitiated "$baseline_initiated" '
                [.data.repository.pullRequest.reviewThreads.nodes[]
                  | select(.isResolved == false)
                  | select(.comments.nodes[0].author.login == "chatgpt-codex-connector")
                  | .id
                ] as $codex_unresolved
                | ($codex_unresolved - $baselineInitiated) | length'); then
              echo "::warning::Could not re-check for new PR-wide concerns — leaving remaining threads as-is: $(cat /tmp/fresh-check-err.log)"
              break
            fi
            if [ "$fresh_check" -gt 0 ]; then
              echo "Codex raised a new concern elsewhere on the PR while this loop was running — leaving all remaining threads as-is for human review."
              break
            fi
            # Re-check this thread's LIVE state immediately before mutating
            # it — the $result snapshot above is a single point-in-time
            # query, and a human can reply to (or a maintainer can resolve)
            # a specific thread in the gap between that snapshot and this
            # loop iteration reaching it, especially with many threads to
            # walk. Trusting the stale snapshot would let this loop silently
            # override a human's concern that arrived moments earlier —
            # violating this workflow's own stated invariant that a
            # human-joined thread is never auto-resolved (Codex P2, flagged
            # reviewing this file). Freshness (no comment since RUN_STARTED)
            # is re-checked here too, not just authorship/resolved-state —
            # the earlier version of this live recheck only re-verified
            # authorship purity, so Codex restating a concern IN this same
            # thread (rather than opening a new one) between the snapshot
            # and this loop reaching it stayed invisible: same authors,
            # still unresolved, but with a fresh comment the loop never
            # looked for (Codex P2, direct follow-up flagged reviewing this
            # file's own live-recheck fix).
            # Guarded like the reply/resolve mutations below it (skip this
            # thread, don't abort the step): an unguarded assignment here
            # would let one transient GraphQL/auth/rate-limit failure crash
            # the whole required check before any thread gets processed,
            # even though the polling query above already treats the exact
            # same failure class as retry-worthy (Codex P2, flagged
            # reviewing this file).
            if ! live=$(gh api graphql -f query='
              query($id: ID!) {
                node(id: $id) {
                  ... on PullRequestReviewThread {
                    isResolved
                    comments(first: 50) { nodes { author { login } createdAt } }
                  }
                }
              }' -f id="$thread_id" 2>/tmp/live-check-err.log); then
              echo "::warning::Could not re-check thread $thread_id's live state — leaving unresolved: $(cat /tmp/live-check-err.log)"
              continue
            fi
            live_resolved=$(echo "$live" | jq -r '.data.node.isResolved')
            live_pure=$(echo "$live" | jq '[.data.node.comments.nodes[].author.login] | all(. == "chatgpt-codex-connector" or . == "github-actions")')
            live_fresh=$(echo "$live" | jq --arg since "$RUN_STARTED" '[.data.node.comments.nodes[] | select(.createdAt >= $since)] | length == 0')
            if [ "$live_resolved" != "false" ] || [ "$live_pure" != "true" ] || [ "$live_fresh" != "true" ]; then
              echo "Skipping thread $thread_id — no longer eligible (resolved, human-touched, or a fresh comment landed since the snapshot)."
              continue
            fi
            if ! gh api graphql -f query='
              mutation($id: ID!, $body: String!) {
                addPullRequestReviewThreadReply(input: {pullRequestReviewThreadId: $id, body: $body}) {
                  comment { id }
                }
              }' -f id="$thread_id" -f body="Auto-resolved: Codex re-reviewed commit $HEAD_SHA after this push and raised no new concerns." 2>/tmp/reply-err.log; then
              echo "::warning::Could not reply to thread $thread_id — leaving unresolved: $(cat /tmp/reply-err.log)"
              continue
            fi

            # A second live recheck, immediately before the resolve mutation
            # below: the first recheck above only covers the gap up to THIS
            # loop iteration reaching it, but two more network round-trips
            # (the reply just posted, and the resolve call next) each give a
            # human another window to reply before the thread is actually
            # marked resolved — the single recheck above doesn't close that
            # (Codex P2, flagged reviewing this file on watchfinder#33). Our
            # own just-posted reply is authored "github-actions", which the
            # purity test already treats as ignorable, so it doesn't trip
            # live_pure on its own.
            if ! live2=$(gh api graphql -f query='
              query($id: ID!) {
                node(id: $id) {
                  ... on PullRequestReviewThread {
                    isResolved
                    comments(first: 50) { nodes { author { login } createdAt } }
                  }
                }
              }' -f id="$thread_id" 2>/tmp/live2-check-err.log); then
              # Guarded like the first live recheck above: an unguarded
              # assignment here would let one transient GraphQL/auth/
              # rate-limit failure on THIS SECOND query abort the whole step
              # before any remaining thread gets processed, even though the
              # reply just above already landed successfully (Codex P2,
              # flagged reviewing this file on sunto's sync PR).
              echo "::warning::Could not re-check thread $thread_id's live state before resolving — leaving the reply in place, unresolved: $(cat /tmp/live2-check-err.log)"
              continue
            fi
            live2_resolved=$(echo "$live2" | jq -r '.data.node.isResolved')
            live2_pure=$(echo "$live2" | jq '[.data.node.comments.nodes[].author.login] | all(. == "chatgpt-codex-connector" or . == "github-actions")')
            # Same freshness test as the first recheck, re-applied here: our
            # own just-posted reply is always >= RUN_STARTED and already
            # excluded from tripping this via live2_pure, but a Codex
            # restatement or human reply landing in the reply-to-resolve
            # gap specifically is not caught by isResolved/purity alone
            # (Codex P2, flagged reviewing this file on banco's sync PR —
            # direct follow-up to watchfinder#33's live2 recheck, which
            # fetched createdAt but never tested it).
            live2_fresh=$(echo "$live2" | jq --arg since "$RUN_STARTED" '[.data.node.comments.nodes[] | select(.author.login != "github-actions" and .createdAt >= $since)] | length == 0')
            if [ "$live2_resolved" != "false" ] || [ "$live2_pure" != "true" ] || [ "$live2_fresh" != "true" ]; then
              echo "Skipping resolve for thread $thread_id — became ineligible between reply and resolve (left the reply in place)."
              continue
            fi
            # GITHUB_TOKEN (github-actions[bot]) reliably fails this specific
            # mutation on a bot-started thread with "Resource not accessible
            # by integration" — confirmed live on pancetta#239, 10/10 resolve
            # attempts failed this way even though the reply above succeeded
            # every time. RESOLVE_TOKEN (a real-user PAT) does not have this
            # restriction. Kept as a soft-skip on failure rather than crashing
            # the whole required check, in case RESOLVE_TOKEN itself is ever
            # unset/revoked — the reply above stays authored as
            # "github-actions" regardless of which token resolves it, which
            # the ignorable-author filters above already treat as
            # non-blocking, so a thread that still fails here stays eligible
            # for a clean resolve on the next round instead of becoming
            # permanently manual (Codex P3).
            if ! GH_TOKEN="$RESOLVE_TOKEN" gh api graphql -f query='
              mutation($id: ID!) {
                resolveReviewThread(input: {threadId: $id}) {
                  thread { isResolved }
                }
              }' -f id="$thread_id" 2>/tmp/resolve-err.log; then
              echo "::warning::Could not resolve thread $thread_id (left a reply; may need manual resolution): $(cat /tmp/resolve-err.log)"
              continue
            fi
            echo "Auto-resolved thread $thread_id"
          done
